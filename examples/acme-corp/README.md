# Acme Corp Demo Corpus

This corpus gives `phase2d` a stable, themed set of related artifacts for local demos and CI smoke tests.

The current runtime is still text-first, so some files use `.pptx`, `.docx`, and `.pdf` extensions with plain-text fixture content. That keeps the continuity of the source shapes while the real binary/document extractors are still being added.
